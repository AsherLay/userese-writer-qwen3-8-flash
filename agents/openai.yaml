interface:
  display_name: "Userese Writer · Qwen3.8-Flash"
  short_description: "用 Qwen3.8-Flash 生成待核实的 Userese 文案提案 / Write checkable Userese copy proposals with Qwen3.8-Flash"
  default_prompt: "使用 $userese-writer-qwen3-8-flash 处理我已确认的 Userese brief，只生成 before/after 提案。"
policy:
  allow_implicit_invocation: false
